using Microsoft.AspNetCore.SignalR;

namespace HP_Detailing.Hubs
{
    public class NotificationHub : Hub
    {
        // The hub can be empty as we'll trigger notifications from the server side via IHubContext
    }
}
